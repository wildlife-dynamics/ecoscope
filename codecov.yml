coverage:
  status:
    project:
      default:
        # Require core reports before evaluating project coverage;
        # io/smart-io/daily are conditional on secrets being available
        flags:
          - core-non-platform
          - core-platform

flag_management:
  default_rules:
    carryforward: true
